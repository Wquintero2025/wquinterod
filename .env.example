# Copy this to .env (for local dev) or configure env in Dokploy
DATABASE_URL=postgresql://app:app_pwd@localhost:5432/saas?schema=public
BASE_DOMAIN=localhost
NEXT_PUBLIC_BASE_DOMAIN=localhost
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change_me
EMAIL_FROM=no-reply@localhost
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
